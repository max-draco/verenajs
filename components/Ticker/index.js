import s from './index.module.css';
export function createTicker({items=[],speed=50,pauseOnHover=true}){const c=document.createElement('div');c.className=s.ticker;const track=document.createElement('div');track.className=s.track;const content=items.map(i=>`<span class="${s.item}"><span class="${s.name}">${i.name}</span><span class="${s.price}">${i.price}</span><span class="${s.change} ${i.change>=0?s.up:s.down}">${i.change>=0?'+':''}${i.change}%</span></span>`).join('');track.innerHTML=content+content;c.appendChild(track);track.style.animationDuration=`${items.length*speed/10}s`;if(pauseOnHover){c.onmouseenter=()=>track.style.animationPlayState='paused';c.onmouseleave=()=>track.style.animationPlayState='running';}return c;}