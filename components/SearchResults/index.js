import s from './index.module.css';
export function createSearchResults({results=[],query='',onResultClick=()=>{}}){const c=document.createElement('div');c.className=s.results;if(!results.length){c.innerHTML=`<div class="${s.empty}">No results for "${query}"</div>`;return c;}c.innerHTML=`<div class="${s.count}">${results.length} results for "${query}"</div><div class="${s.list}">${results.map(r=>`<div class="${s.item}" data-id="${r.id}"><div class="${s.title}">${r.title}</div>${r.description?`<div class="${s.desc}">${r.description}</div>`:''}</div>`).join('')}</div>`;c.querySelectorAll('.'+s.item).forEach(el=>el.onclick=()=>onResultClick(el.dataset.id));return c;}