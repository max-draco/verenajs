import s from './index.module.css';
export function createFontPicker({fonts=['Inter','Roboto','Open Sans','Lato','Montserrat'],selected='',onChange=()=>{}}){const c=document.createElement('div');c.className=s.picker;fonts.forEach(font=>{const el=document.createElement('button');el.className=`${s.font} ${font===selected?s.selected:''}`;el.style.fontFamily=font;el.innerHTML=`<span class="${s.name}">${font}</span><span class="${s.preview}">Aa</span>`;el.onclick=()=>{c.querySelectorAll('.'+s.font).forEach(b=>b.classList.remove(s.selected));el.classList.add(s.selected);onChange(font);};c.appendChild(el);});return c;}