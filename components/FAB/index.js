import s from './index.module.css';export function createFAB({icon='➕',onClick=()=>{},actions=[]}){const c=document.createElement('div');c.className=s.fab;c.innerHTML=`<button class="${s.main}">${icon}</button>${actions.length?`<div class="${s.actions}">${actions.map(a=>`<button class="${s.action}" data-label="${a.label}">${a.icon}</button>`).join('')}</div>`:''}`;c.querySelector('.'+s.main).onclick=()=>{if(actions.length)c.classList.toggle(s.open);else onClick();};actions.forEach((a,i)=>c.querySelectorAll('.'+s.action)[i].onclick=a.onClick);return c;}