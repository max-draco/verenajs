import s from './index.module.css';
export function createMarketInfo({rules='',resolution='',sources=[],createdAt='',expiresAt=''}){const c=document.createElement('div');c.className=s.info;c.innerHTML=`<div class="${s.section}"><h4>Resolution Rules</h4><p>${rules}</p></div><div class="${s.section}"><h4>Resolution Source</h4><p>${resolution}</p></div>${sources.length?`<div class="${s.section}"><h4>Sources</h4><ul>${sources.map(src=>`<li><a href="${src.url}" target="_blank">${src.name}</a></li>`).join('')}</ul></div>`:''}<div class="${s.meta}"><span>Created: ${createdAt}</span><span>Expires: ${expiresAt}</span></div>`;return c;}