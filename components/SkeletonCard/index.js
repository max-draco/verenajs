import s from './index.module.css';
export function createSkeletonCard({lines=3,hasImage=false,hasAvatar=false}){const c=document.createElement('div');c.className=s.card;let html='';if(hasImage)html+=`<div class="${s.image} ${s.shimmer}"></div>`;if(hasAvatar)html+=`<div class="${s.header}"><div class="${s.avatar} ${s.shimmer}"></div><div class="${s.meta}"><div class="${s.line} ${s.shimmer}" style="width:60%"></div><div class="${s.line} ${s.shimmer}" style="width:40%"></div></div></div>`;for(let i=0;i<lines;i++)html+=`<div class="${s.line} ${s.shimmer}" style="width:${100-i*15}%"></div>`;c.innerHTML=html;return c;}