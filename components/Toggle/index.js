import s from './index.module.css';
export function createToggle({options=[],value='',onChange=()=>{}}){const c=document.createElement('div');c.className=s.toggle;options.forEach(opt=>{const btn=document.createElement('button');btn.className=`${s.option} ${opt.value===value?s.active:''}`;btn.textContent=opt.label;btn.onclick=()=>{value=opt.value;c.querySelectorAll('.'+s.option).forEach((b,i)=>b.classList.toggle(s.active,options[i].value===value));onChange(value);};c.appendChild(btn);});return{el:c,getValue:()=>value,setValue:v=>{value=v;c.querySelectorAll('.'+s.option).forEach((b,i)=>b.classList.toggle(s.active,options[i].value===v));}};}