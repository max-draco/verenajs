import s from './index.module.css';
export function createMenu({items=[],onSelect=()=>{}}){const c=document.createElement('div');c.className=s.menu;items.forEach(item=>{if(item.divider){const d=document.createElement('div');d.className=s.divider;c.appendChild(d);return;}const el=document.createElement('div');el.className=`${s.item} ${item.disabled?s.disabled:''}`;el.innerHTML=`${item.icon?`<span class="${s.icon}">${item.icon}</span>`:''}<span class="${s.label}">${item.label}</span>${item.shortcut?`<span class="${s.shortcut}">${item.shortcut}</span>`:''}`;if(!item.disabled)el.onclick=()=>onSelect(item);c.appendChild(el);});return c;}