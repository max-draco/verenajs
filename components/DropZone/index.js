import s from './index.module.css';
export function createDropZone({accept='*',multiple=true,onDrop=()=>{}}){const c=document.createElement('div');c.className=s.dropzone;c.innerHTML=`<input type="file" class="${s.input}" ${accept!=='*'?`accept="${accept}"`:''} ${multiple?'multiple':''}><div class="${s.content}"><div class="${s.icon}">📁</div><div class="${s.text}">Drop files here or click to upload</div><div class="${s.hint}">Supports ${accept}</div></div>`;const input=c.querySelector('input');c.onclick=()=>input.click();c.ondragover=e=>{e.preventDefault();c.classList.add(s.active);};c.ondragleave=()=>c.classList.remove(s.active);c.ondrop=e=>{e.preventDefault();c.classList.remove(s.active);onDrop([...e.dataTransfer.files]);};input.onchange=()=>onDrop([...input.files]);return c;}