import s from './index.module.css';
export function createConversionFunnel({steps=[]}){const c=document.createElement('div');c.className=s.funnel;const max=steps[0]?.value||1;steps.forEach((step,i)=>{const pct=(step.value/max)*100;const conv=i>0?((step.value/steps[i-1].value)*100).toFixed(1)+'%':'100%';const el=document.createElement('div');el.className=s.step;el.innerHTML=`<div class="${s.bar}" style="width:${pct}%"><span class="${s.value}">${step.value.toLocaleString()}</span></div><div class="${s.info}"><span class="${s.label}">${step.label}</span><span class="${s.conv}">${conv}</span></div>`;c.appendChild(el);});return c;}