import s from './index.module.css';
export function createMarketCard({id='',title='',description='',yesPrice=0.5,noPrice=0.5,volume=0,liquidity=0,endDate='',category='',image='',trending=false,onYes=()=>{},onNo=()=>{},onClick=()=>{}}){const c=document.createElement('div');c.className=s.card;c.innerHTML=`${image?`<div class="${s.image}" style="background-image:url(${image})"></div>`:''}${trending?`<span class="${s.trending}">🔥 Trending</span>`:''}<div class="${s.category}">${category}</div><h3 class="${s.title}">${title}</h3><p class="${s.desc}">${description}</p><div class="${s.prices}"><button class="${s.yesBtn}"><span class="${s.label}">Yes</span><span class="${s.price}">${(yesPrice*100).toFixed(0)}¢</span></button><button class="${s.noBtn}"><span class="${s.label}">No</span><span class="${s.price}">${(noPrice*100).toFixed(0)}¢</span></button></div><div class="${s.stats}"><span>$${(volume/1000).toFixed(0)}k Vol</span><span>$${(liquidity/1000).toFixed(0)}k Liq</span></div>`;c.querySelector('.'+s.yesBtn).onclick=e=>{e.stopPropagation();onYes({id,title,yesPrice});};c.querySelector('.'+s.noBtn).onclick=e=>{e.stopPropagation();onNo({id,title,noPrice});};c.onclick=()=>onClick({id,title});return c;}