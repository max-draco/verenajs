import s from './index.module.css';
export function createImageGallery({images=[],columns=3}){const c=document.createElement('div');c.className=s.gallery;c.style.gridTemplateColumns=`repeat(${columns},1fr)`;images.forEach((img,i)=>{const el=document.createElement('div');el.className=s.item;el.innerHTML=`<img src="${img.src}" alt="${img.alt||''}">`;el.onclick=()=>openLightbox(i);c.appendChild(el);});const openLightbox=(idx)=>{const lb=document.createElement('div');lb.className=s.lightbox;lb.innerHTML=`<button class="${s.close}">×</button><button class="${s.prev}">‹</button><img src="${images[idx].src}"><button class="${s.next}">›</button>`;lb.querySelector('.'+s.close).onclick=()=>lb.remove();lb.querySelector('.'+s.prev).onclick=()=>{idx=(idx-1+images.length)%images.length;lb.querySelector('img').src=images[idx].src;};lb.querySelector('.'+s.next).onclick=()=>{idx=(idx+1)%images.length;lb.querySelector('img').src=images[idx].src;};document.body.appendChild(lb);};return c;}