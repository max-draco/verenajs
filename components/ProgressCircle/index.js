import s from './index.module.css';
export function createProgressCircle({value=0,max=100,size=120,strokeWidth=8,color='#3b82f6',showLabel=true}){const c=document.createElement('div');c.className=s.circle;c.style.width=size+'px';c.style.height=size+'px';const radius=(size-strokeWidth)/2;const circumference=radius*2*Math.PI;const pct=Math.min(value/max,1);const offset=circumference-(pct*circumference);c.innerHTML=`<svg viewBox="0 0 ${size} ${size}"><circle cx="${size/2}" cy="${size/2}" r="${radius}" fill="none" stroke="#2a2a4e" stroke-width="${strokeWidth}"/><circle cx="${size/2}" cy="${size/2}" r="${radius}" fill="none" stroke="${color}" stroke-width="${strokeWidth}" stroke-dasharray="${circumference}" stroke-dashoffset="${offset}" stroke-linecap="round" transform="rotate(-90 ${size/2} ${size/2})"/></svg>${showLabel?`<div class="${s.label}">${Math.round(pct*100)}%</div>`:''}`;return c;}