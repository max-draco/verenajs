import s from './index.module.css';
export function createGaugeChart({value=0,min=0,max=100,label='',color='#3b82f6'}){const c=document.createElement('div');c.className=s.gauge;const pct=Math.min(Math.max((value-min)/(max-min),0),1);const angle=pct*180-90;const size=160,cx=size/2,cy=size/2,r=60;const arcPath=(start,end,radius)=>{const s1=start*Math.PI/180,e1=end*Math.PI/180;const x1=cx+radius*Math.cos(s1),y1=cy+radius*Math.sin(s1);const x2=cx+radius*Math.cos(e1),y2=cy+radius*Math.sin(e1);const large=end-start>180?1:0;return`M${x1},${y1} A${radius},${radius} 0 ${large},1 ${x2},${y2}`;};c.innerHTML=`<svg viewBox="0 0 ${size} ${size*0.6}" class="${s.svg}"><path d="${arcPath(-180,0,r)}" fill="none" stroke="#2a2a4e" stroke-width="12" stroke-linecap="round"/><path d="${arcPath(-180,-180+pct*180,r)}" fill="none" stroke="${color}" stroke-width="12" stroke-linecap="round"/><text x="${cx}" y="${cy+10}" text-anchor="middle" fill="#f6f6f7" font-size="24" font-weight="700">${value}</text><text x="${cx}" y="${cy+28}" text-anchor="middle" fill="#6b7280" font-size="12">${label}</text></svg>`;return c;}