import s from './index.module.css';
export function createWeekView({events=[],startDate=new Date(),onEventClick=()=>{}}){const c=document.createElement('div');c.className=s.weekview;const days=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];const hours=Array.from({length:24},(_, i)=>i);let html=`<div class="${s.header}"><div class="${s.timeCol}"></div>`;for(let i=0;i<7;i++){const d=new Date(startDate);d.setDate(d.getDate()+i);html+=`<div class="${s.dayCol}"><span class="${s.dayName}">${days[d.getDay()]}</span><span class="${s.dayNum}">${d.getDate()}</span></div>`;}html+=`</div><div class="${s.body}">`;hours.forEach(h=>{html+=`<div class="${s.row}"><div class="${s.time}">${h}:00</div>`;for(let i=0;i<7;i++)html+=`<div class="${s.cell}"></div>`;html+=`</div>`;});html+=`</div>`;c.innerHTML=html;return c;}