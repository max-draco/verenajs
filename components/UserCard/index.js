import s from './index.module.css';
export function createUserCard({user={},stats=[],onFollow=()=>{},isFollowing=false}){const c=document.createElement('div');c.className=s.card;c.innerHTML=`<div class="${s.cover}" style="background:${user.coverColor||'linear-gradient(135deg,#3b82f6,#8b5cf6)'}"></div><img class="${s.avatar}" src="${user.avatar||''}" alt=""><div class="${s.info}"><h3 class="${s.name}">${user.name}</h3><p class="${s.bio}">${user.bio||''}</p></div><div class="${s.stats}">${stats.map(st=>`<div class="${s.stat}"><div class="${s.statValue}">${st.value}</div><div class="${s.statLabel}">${st.label}</div></div>`).join('')}</div><button class="${s.follow} ${isFollowing?s.following:''}">${isFollowing?'Following':'Follow'}</button>`;c.querySelector('.'+s.follow).onclick=onFollow;return c;}