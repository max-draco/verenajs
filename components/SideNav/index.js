import s from './index.module.css';
export function createSideNav({items=[],activeId='',collapsed=false,onSelect=()=>{},onToggle=()=>{}}){const c=document.createElement('nav');c.className=`${s.sidenav} ${collapsed?s.collapsed:''}`;const toggle=document.createElement('button');toggle.className=s.toggle;toggle.innerHTML='☰';toggle.onclick=()=>{collapsed=!collapsed;c.classList.toggle(s.collapsed,collapsed);onToggle(collapsed);};c.appendChild(toggle);items.forEach(item=>{if(item.section){const sec=document.createElement('div');sec.className=s.section;sec.textContent=item.label;c.appendChild(sec);return;}const btn=document.createElement('button');btn.className=`${s.item} ${item.id===activeId?s.active:''}`;btn.innerHTML=`<span class="${s.icon}">${item.icon}</span><span class="${s.label}">${item.label}</span>`;btn.onclick=()=>onSelect(item.id);c.appendChild(btn);});return{el:c,setActive:(id)=>{c.querySelectorAll('.'+s.item).forEach(el=>el.classList.toggle(s.active,el.querySelector('.'+s.label)?.textContent===items.find(i=>i.id===id)?.label));}};}