import s from './index.module.css';
export function createMegaMenu({sections=[],onSelect=()=>{}}){const c=document.createElement('div');c.className=s.megamenu;sections.forEach(section=>{const col=document.createElement('div');col.className=s.section;col.innerHTML=`<div class="${s.title}">${section.title}</div>`;section.items.forEach(item=>{const el=document.createElement('a');el.className=s.item;el.href=item.href||'#';el.innerHTML=`${item.icon?`<span class="${s.icon}">${item.icon}</span>`:''}<div class="${s.content}"><div class="${s.label}">${item.label}</div>${item.description?`<div class="${s.desc}">${item.description}</div>`:''}</div>`;el.onclick=e=>{e.preventDefault();onSelect(item);};col.appendChild(el);});c.appendChild(col);});return c;}