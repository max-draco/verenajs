import s from './index.module.css';
export function createOnboarding({slides=[],onComplete=()=>{}}){const c=document.createElement('div');c.className=s.onboarding;let current=0;const render=()=>{const slide=slides[current];c.innerHTML=`<div class="${s.slide}"><div class="${s.icon}">${slide.icon||'🚀'}</div><h2 class="${s.title}">${slide.title}</h2><p class="${s.desc}">${slide.description}</p></div><div class="${s.dots}">${slides.map((_,i)=>`<span class="${s.dot} ${i===current?s.active:''}"></span>`).join('')}</div><div class="${s.actions}">${current>0?`<button class="${s.back}">Back</button>`:''}<button class="${s.next}">${current===slides.length-1?'Get Started':'Next'}</button></div>`;if(current>0)c.querySelector('.'+s.back).onclick=()=>{current--;render();};c.querySelector('.'+s.next).onclick=()=>{if(current<slides.length-1){current++;render();}else onComplete();};};render();return{el:c,reset:()=>{current=0;render();}};}