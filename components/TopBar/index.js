import s from './index.module.css';
export function createTopBar({logo='',title='',actions=[],onLogoClick=()=>{}}){const c=document.createElement('header');c.className=s.topbar;c.innerHTML=`<div class="${s.left}"><div class="${s.logo}" role="button">${logo}</div><span class="${s.title}">${title}</span></div><div class="${s.actions}"></div>`;c.querySelector('.'+s.logo).onclick=onLogoClick;const actionsContainer=c.querySelector('.'+s.actions);actions.forEach(action=>{const btn=document.createElement('button');btn.className=s.action;btn.innerHTML=action.icon||action.label;btn.onclick=action.onClick;actionsContainer.appendChild(btn);});return c;}