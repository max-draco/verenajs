import s from './index.module.css';
export function createCountdownTimer({targetDate,onComplete=()=>{}}){const c=document.createElement('div');c.className=s.timer;const update=()=>{const diff=new Date(targetDate)-new Date();if(diff<=0){c.innerHTML='<span class="'+s.ended+'">Ended</span>';onComplete();return;}const d=Math.floor(diff/86400000);const h=Math.floor((diff%86400000)/3600000);const m=Math.floor((diff%3600000)/60000);const sec=Math.floor((diff%60000)/1000);c.innerHTML=`<div class="${s.unit}"><span class="${s.val}">${d}</span><span class="${s.lbl}">Days</span></div><div class="${s.unit}"><span class="${s.val}">${h}</span><span class="${s.lbl}">Hours</span></div><div class="${s.unit}"><span class="${s.val}">${m}</span><span class="${s.lbl}">Min</span></div><div class="${s.unit}"><span class="${s.val}">${sec}</span><span class="${s.lbl}">Sec</span></div>`;};update();const iv=setInterval(update,1000);return{el:c,destroy:()=>clearInterval(iv)};}