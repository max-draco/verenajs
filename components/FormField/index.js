import s from './index.module.css';
export function createFormField({label='',required=false,error='',hint='',children=null}){const c=document.createElement('div');c.className=`${s.field} ${error?s.hasError:''}`;c.innerHTML=`<label class="${s.label}">${label}${required?`<span class="${s.required}">*</span>`:''}</label><div class="${s.content}"></div>${hint&&!error?`<span class="${s.hint}">${hint}</span>`:''}${error?`<span class="${s.error}">${error}</span>`:''}`;if(children)c.querySelector('.'+s.content).appendChild(children);return{el:c,setError:e=>{const errEl=c.querySelector('.'+s.error);if(errEl)errEl.textContent=e;else if(e){const span=document.createElement('span');span.className=s.error;span.textContent=e;c.appendChild(span);}c.classList.toggle(s.hasError,!!e);}};}