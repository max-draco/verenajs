import s from './index.module.css';
export function createBetSlip({bets=[],onRemove=()=>{},onClear=()=>{},onSubmit=()=>{}}){const c=document.createElement('div');c.className=s.slip;const total=bets.reduce((a,b)=>a+b.stake,0);const potentialWin=bets.reduce((a,b)=>a+(b.stake*(100/b.odds)-b.stake),0);c.innerHTML=`<div class="${s.header}"><span>Bet Slip</span><span class="${s.count}">${bets.length}</span></div><div class="${s.bets}">${bets.map(b=>`<div class="${s.bet}" data-id="${b.id}"><div class="${s.betHeader}"><span class="${s.market}">${b.market}</span><button class="${s.remove}">×</button></div><div class="${s.selection}"><span class="${s.side} ${b.side==='yes'?s.yes:s.no}">${b.side.toUpperCase()}</span><span class="${s.odds}">${b.odds}¢</span></div><input type="number" class="${s.stake}" value="${b.stake}" min="1" placeholder="Stake"></div>`).join('')}</div><div class="${s.summary}"><div class="${s.row}"><span>Total Stake</span><span>$${total.toFixed(2)}</span></div><div class="${s.row}"><span>Potential Win</span><span class="${s.win}">+$${potentialWin.toFixed(2)}</span></div></div><div class="${s.actions}"><button class="${s.clear}">Clear All</button><button class="${s.submit}">Place Bets</button></div>`;c.querySelectorAll('.'+s.remove).forEach(btn=>btn.onclick=e=>{onRemove(e.target.closest('.'+s.bet).dataset.id);});c.querySelector('.'+s.clear).onclick=onClear;c.querySelector('.'+s.submit).onclick=()=>onSubmit(bets);return c;}