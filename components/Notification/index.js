import s from './index.module.css';
export function createNotification({type='info',title='',message='',duration=5000,onClose=()=>{}}){const c=document.createElement('div');c.className=`${s.notification} ${s[type]}`;c.innerHTML=`<div class="${s.icon}">${{info:'ℹ️',success:'✅',warning:'⚠️',error:'❌'}[type]}</div><div class="${s.content}"><div class="${s.title}">${title}</div><div class="${s.message}">${message}</div></div><button class="${s.close}">×</button>`;c.querySelector('.'+s.close).onclick=()=>{c.classList.add(s.exit);setTimeout(()=>{c.remove();onClose();},300);};if(duration>0)setTimeout(()=>c.querySelector('.'+s.close)?.click(),duration);return c;}