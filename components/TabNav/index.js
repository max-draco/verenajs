import s from './index.module.css';
export function createTabNav({tabs=[],activeId='',variant='default',onChange=()=>{}}){const c=document.createElement('div');c.className=`${s.tabnav} ${s[variant]}`;tabs.forEach(tab=>{const btn=document.createElement('button');btn.className=`${s.tab} ${tab.id===activeId?s.active:''}`;btn.innerHTML=`${tab.icon?`<span class="${s.icon}">${tab.icon}</span>`:''}${tab.label}${tab.badge?`<span class="${s.badge}">${tab.badge}</span>`:''}`;btn.onclick=()=>onChange(tab.id);c.appendChild(btn);});return{el:c,setActive:(id)=>{c.querySelectorAll('.'+s.tab).forEach((t,i)=>t.classList.toggle(s.active,tabs[i].id===id));}};}