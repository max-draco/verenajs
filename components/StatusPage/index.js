import s from './index.module.css';
export function createStatusPage({services=[],incidents=[]}){const c=document.createElement('div');c.className=s.status;const allOk=services.every(svc=>svc.status==='operational');c.innerHTML=`<div class="${s.header} ${allOk?s.ok:s.issue}"><span class="${s.indicator}"></span><span>${allOk?'All Systems Operational':'Some Systems Degraded'}</span></div><div class="${s.services}">${services.map(svc=>`<div class="${s.service}"><span class="${s.name}">${svc.name}</span><span class="${s.badge} ${s[svc.status]}">${svc.status}</span></div>`).join('')}</div>${incidents.length?`<div class="${s.incidents}"><h3>Recent Incidents</h3>${incidents.map(inc=>`<div class="${s.incident}"><span class="${s.incDate}">${inc.date}</span><span class="${s.incTitle}">${inc.title}</span></div>`).join('')}</div>`:''}`; return c;}