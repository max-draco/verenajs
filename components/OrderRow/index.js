import s from './index.module.css';
export function createOrderRow({id='',market='',side='yes',type='limit',price=0,amount=0,filled=0,status='open',onCancel=()=>{}}){const c=document.createElement('div');c.className=`${s.row} ${s[status]}`;const pct=(filled/amount)*100;c.innerHTML=`<div class="${s.market}">${market}</div><div class="${s.side} ${side==='yes'?s.yes:s.no}">${side.toUpperCase()}</div><div class="${s.type}">${type}</div><div class="${s.price}">${price}¢</div><div class="${s.amount}"><div class="${s.fillBar}" style="width:${pct}%"></div><span>$${filled.toFixed(2)}/$${amount.toFixed(2)}</span></div><div class="${s.status}">${status}</div>${status==='open'?`<button class="${s.cancel}">Cancel</button>`:''}`;const btn=c.querySelector('.'+s.cancel);if(btn)btn.onclick=()=>onCancel(id);return c;}