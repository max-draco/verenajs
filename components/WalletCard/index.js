import s from './index.module.css';
export function createWalletCard({address='',balance=0,currency='ETH',network='Ethereum',onDisconnect=()=>{}}){const c=document.createElement('div');c.className=s.card;const short=address?`${address.slice(0,6)}...${address.slice(-4)}`:'Not connected';c.innerHTML=`<div class="${s.header}"><span class="${s.network}">${network}</span><button class="${s.disconnect}">Disconnect</button></div><div class="${s.balance}"><span class="${s.amount}">${balance.toFixed(4)}</span><span class="${s.currency}">${currency}</span></div><div class="${s.address}"><span>${short}</span><button class="${s.copy}">📋</button></div>`;c.querySelector('.'+s.disconnect).onclick=onDisconnect;c.querySelector('.'+s.copy).onclick=()=>{navigator.clipboard.writeText(address);};return c;}