import s from './index.module.css';
export function createTransactionList({transactions=[],onItemClick=()=>{}}){const c=document.createElement('div');c.className=s.list;transactions.forEach(tx=>{const el=document.createElement('div');el.className=s.item;const icon=tx.type==='buy'?'📥':tx.type==='sell'?'📤':'🔄';const color=tx.type==='buy'?'#22c55e':tx.type==='sell'?'#ef4444':'#3b82f6';el.innerHTML=`<div class="${s.icon}" style="background:${color}20;color:${color}">${icon}</div><div class="${s.info}"><div class="${s.title}">${tx.title}</div><div class="${s.meta}">${tx.date} • ${tx.status}</div></div><div class="${s.amount}" style="color:${color}">${tx.type==='buy'?'+':'-'}$${tx.amount.toFixed(2)}</div>`;el.onclick=()=>onItemClick(tx);c.appendChild(el);});return c;}