import s from './index.module.css';
export function createConfirmDialog({title='Confirm',message='',confirmText='Confirm',cancelText='Cancel',variant='default',onConfirm=()=>{},onCancel=()=>{}}){const c=document.createElement('div');c.className=s.overlay;c.innerHTML=`<div class="${s.dialog}"><h3 class="${s.title}">${title}</h3><p class="${s.message}">${message}</p><div class="${s.actions}"><button class="${s.cancel}">${cancelText}</button><button class="${s.confirm} ${s[variant]}">${confirmText}</button></div></div>`;c.querySelector('.'+s.cancel).onclick=()=>{c.remove();onCancel();};c.querySelector('.'+s.confirm).onclick=()=>{c.remove();onConfirm();};document.body.appendChild(c);return{close:()=>c.remove()};}