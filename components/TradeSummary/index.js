import s from './index.module.css';
export function createTradeSummary({trades=0,volume=0,avgPrice=0,profit=0}){const c=document.createElement('div');c.className=s.summary;c.innerHTML=`<div class="${s.stat}"><span class="${s.label}">Trades</span><span class="${s.value}">${trades}</span></div><div class="${s.stat}"><span class="${s.label}">Volume</span><span class="${s.value}">$${volume.toLocaleString()}</span></div><div class="${s.stat}"><span class="${s.label}">Avg Price</span><span class="${s.value}">${avgPrice}¢</span></div><div class="${s.stat}"><span class="${s.label}">P&L</span><span class="${s.value} ${profit>=0?s.profit:s.loss}">${profit>=0?'+':''}$${profit.toFixed(2)}</span></div>`;return c;}