import s from './index.module.css';
export function createDashboardCard({title='',value='',subtitle='',icon='',trend=null,chart=null,actions=[]}){const c=document.createElement('div');c.className=s.card;c.innerHTML=`<div class="${s.header}"><div class="${s.info}"><span class="${s.title}">${title}</span>${subtitle?`<span class="${s.subtitle}">${subtitle}</span>`:''}</div>${icon?`<div class="${s.icon}">${icon}</div>`:''}</div><div class="${s.body}"><span class="${s.value}">${value}</span>${trend!==null?`<span class="${s.trend} ${trend>=0?s.up:s.down}">${trend>=0?'↑':'↓'}${Math.abs(trend)}%</span>`:''}</div>${chart?`<div class="${s.chart}"></div>`:''}${actions.length?`<div class="${s.actions}">${actions.map(a=>`<button class="${s.action}">${a.label}</button>`).join('')}</div>`:''}`;actions.forEach((a,i)=>c.querySelectorAll('.'+s.action)[i]?.addEventListener('click',a.onClick));return c;}