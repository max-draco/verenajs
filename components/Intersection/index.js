export function createIntersection({threshold=0.1,rootMargin='0px',onEnter=()=>{},onLeave=()=>{}}){const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting)onEnter(entry.target);else onLeave(entry.target);});},{threshold,rootMargin});return{observe:(el)=>observer.observe(el),unobserve:(el)=>observer.unobserve(el),disconnect:()=>observer.disconnect()};}