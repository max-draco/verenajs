import s from './index.module.css';
export function createMarketHeader({title='',description='',category='',volume=0,liquidity=0,endDate='',resolved=false,outcome=null}){const c=document.createElement('div');c.className=s.header;c.innerHTML=`<div class="${s.top}"><span class="${s.category}">${category}</span>${resolved?`<span class="${s.resolved} ${outcome?s.yes:s.no}">${outcome?'Resolved: YES':'Resolved: NO'}</span>`:''}</div><h1 class="${s.title}">${title}</h1><p class="${s.desc}">${description}</p><div class="${s.meta}"><div class="${s.stat}"><span class="${s.label}">Volume</span><span class="${s.value}">$${(volume/1e6).toFixed(2)}M</span></div><div class="${s.stat}"><span class="${s.label}">Liquidity</span><span class="${s.value}">$${(liquidity/1e3).toFixed(0)}K</span></div><div class="${s.stat}"><span class="${s.label}">End Date</span><span class="${s.value}">${endDate}</span></div></div>`;return c;}