import s from './index.module.css';
export function createRevenueChart({current=0,previous=0,data=[],period='This Month'}){const c=document.createElement('div');c.className=s.chart;const change=previous?((current-previous)/previous*100):0;c.innerHTML=`<div class="${s.header}"><div class="${s.label}">Revenue</div><div class="${s.period}">${period}</div></div><div class="${s.values}"><div class="${s.current}">$${current.toLocaleString()}</div><div class="${s.change} ${change>=0?s.up:s.down}">${change>=0?'↑':'↓'} ${Math.abs(change).toFixed(1)}% vs last period</div></div><div class="${s.bars}">${data.map(d=>`<div class="${s.bar}"><div class="${s.barFill}" style="height:${d.pct}%"></div><div class="${s.barLabel}">${d.label}</div></div>`).join('')}</div>`;return c;}