import s from './index.module.css';
export function createRating({value=0,max=5,readonly=false,size='md',onChange=()=>{}}){const c=document.createElement('div');c.className=`${s.rating} ${s[size]}`;let current=value;const render=()=>{c.innerHTML='';for(let i=1;i<=max;i++){const star=document.createElement('span');star.className=`${s.star} ${i<=current?s.filled:''}`;star.textContent='★';if(!readonly){star.onmouseenter=()=>{c.querySelectorAll('.'+s.star).forEach((s,idx)=>s.classList.toggle(s.filled,idx<i));};star.onclick=()=>{current=i;onChange(current);render();};}c.appendChild(star);}};if(!readonly)c.onmouseleave=()=>render();render();return{el:c,getValue:()=>current,setValue:v=>{current=v;render();}};}