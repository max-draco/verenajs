import s from './index.module.css';export function createHeader({title='',subtitle='',actions=[]}){const c=document.createElement('header');c.className=s.header;c.innerHTML=`<div class="${s.content}"><h1 class="${s.title}">${title}</h1>${subtitle?`<p class="${s.subtitle}">${subtitle}</p>`:''}</div><div class="${s.actions}"></div>`;const actionsEl=c.querySelector('.'+s.actions);actions.forEach(a=>actionsEl.appendChild(a));return c;}