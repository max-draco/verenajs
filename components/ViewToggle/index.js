import s from './index.module.css';export function createViewToggle({view='grid',onChange=()=>{}}){const c=document.createElement('div');c.className=s.toggle;c.innerHTML=`<button class="${s.btn} ${view==='grid'?s.active:''}" data-view="grid">▦</button><button class="${s.btn} ${view==='list'?s.active:''}" data-view="list">☰</button>`;c.querySelectorAll('.'+s.btn).forEach(btn=>btn.onclick=()=>{c.querySelectorAll('.'+s.btn).forEach(b=>b.classList.remove(s.active));btn.classList.add(s.active);onChange(btn.dataset.view);});return c;}