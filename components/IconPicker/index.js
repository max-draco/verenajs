import s from './index.module.css';
export function createIconPicker({icons=['⭐','❤️','🔥','✨','🎯','💡','🚀','💎','🎨','📱'],selected='',searchable=true,onChange=()=>{}}){const c=document.createElement('div');c.className=s.picker;let filtered=[...icons];const render=()=>{c.innerHTML=`${searchable?`<input class="${s.search}" placeholder="Search icons...">`:''}<div class="${s.grid}">${filtered.map(i=>`<button class="${s.icon} ${i===selected?s.selected:''}">${i}</button>`).join('')}</div>`;c.querySelectorAll('.'+s.icon).forEach(btn=>btn.onclick=()=>{c.querySelectorAll('.'+s.icon).forEach(b=>b.classList.remove(s.selected));btn.classList.add(s.selected);onChange(btn.textContent);});const inp=c.querySelector('.'+s.search);if(inp)inp.oninput=e=>{filtered=icons.filter(i=>i.includes(e.target.value));render();};};render();return c;}