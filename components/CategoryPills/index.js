import s from './index.module.css';
export function createCategoryPills({categories=[],selected='all',onChange=()=>{}}){const c=document.createElement('div');c.className=s.pills;categories.forEach(cat=>{const pill=document.createElement('button');pill.className=`${s.pill} ${cat.id===selected?s.active:''}`;pill.innerHTML=`${cat.icon?`<span class="${s.icon}">${cat.icon}</span>`:''}${cat.label}${cat.count?`<span class="${s.count}">${cat.count}</span>`:''}`;pill.onclick=()=>{c.querySelectorAll('.'+s.pill).forEach(p=>p.classList.remove(s.active));pill.classList.add(s.active);onChange(cat.id);};c.appendChild(pill);});return c;}