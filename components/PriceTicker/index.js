import s from './index.module.css';
export function createPriceTicker({symbol='',price=0,change=0,volume=0}){const c=document.createElement('div');c.className=s.ticker;const changeClass=change>=0?s.up:s.down;c.innerHTML=`<div class="${s.symbol}">${symbol}</div><div class="${s.price}">$${price.toLocaleString()}</div><div class="${s.change} ${changeClass}">${change>=0?'+':''}${change.toFixed(2)}%</div><div class="${s.volume}">Vol: $${(volume/1e6).toFixed(2)}M</div>`;return{el:c,update:(p,ch)=>{c.querySelector('.'+s.price).textContent='$'+p.toLocaleString();c.querySelector('.'+s.change).textContent=(ch>=0?'+':'')+ch.toFixed(2)+'%';c.querySelector('.'+s.change).className=`${s.change} ${ch>=0?s.up:s.down}`;}};}