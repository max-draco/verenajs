import s from './index.module.css';
export function createTable({columns=[],data=[],striped=true,hoverable=true,onRowClick=null}){const t=document.createElement('table');t.className=`${s.table} ${striped?s.striped:''} ${hoverable?s.hoverable:''}`;const thead=document.createElement('thead');const hr=document.createElement('tr');columns.forEach(c=>{const th=document.createElement('th');th.textContent=c.label;th.style.width=c.width||'auto';hr.appendChild(th);});thead.appendChild(hr);t.appendChild(thead);const tbody=document.createElement('tbody');data.forEach((row,i)=>{const tr=document.createElement('tr');columns.forEach(c=>{const td=document.createElement('td');td.innerHTML=c.render?c.render(row[c.key],row):row[c.key];tr.appendChild(td);});if(onRowClick)tr.onclick=()=>onRowClick(row,i);tbody.appendChild(tr);});t.appendChild(tbody);return t;}