import s from './index.module.css';
export function createFunnelChart({data=[],width=300,height=200}){const c=document.createElement('div');c.className=s.funnel;if(!data.length)return c;const colors=['#3b82f6','#6366f1','#8b5cf6','#a855f7','#d946ef'];const max=data[0]?.value||1;const stepH=height/data.length;let html='';data.forEach((d,i)=>{const pct=d.value/max;const w=pct*100;const col=d.color||colors[i%colors.length];html+=`<div class="${s.step}"><div class="${s.bar}" style="width:${w}%;background:${col}"><span class="${s.value}">${d.value}</span></div><div class="${s.label}">${d.label}</div></div>`;});c.innerHTML=html;return c;}