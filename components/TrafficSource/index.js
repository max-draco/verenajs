import s from './index.module.css';
export function createTrafficSource({sources=[]}){const c=document.createElement('div');c.className=s.traffic;const total=sources.reduce((a,s)=>a+s.value,0);const colors=['#3b82f6','#22c55e','#f59e0b','#ef4444','#8b5cf6'];c.innerHTML=`<div class="${s.header}">Traffic Sources</div><div class="${s.list}">${sources.map((src,i)=>`<div class="${s.item}"><div class="${s.dot}" style="background:${colors[i%colors.length]}"></div><div class="${s.name}">${src.name}</div><div class="${s.value}">${src.value.toLocaleString()}</div><div class="${s.pct}">${((src.value/total)*100).toFixed(1)}%</div></div>`).join('')}</div><div class="${s.bar}">${sources.map((src,i)=>`<div style="width:${(src.value/total)*100}%;background:${colors[i%colors.length]}"></div>`).join('')}</div>`;return c;}