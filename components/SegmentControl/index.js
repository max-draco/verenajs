import s from './index.module.css';
export function createSegmentControl({segments=[],selected=0,onChange=()=>{}}){const c=document.createElement('div');c.className=s.control;c.style.setProperty('--segments',segments.length);const indicator=document.createElement('div');indicator.className=s.indicator;indicator.style.left=`${(selected/segments.length)*100}%`;c.appendChild(indicator);segments.forEach((seg,i)=>{const btn=document.createElement('button');btn.className=`${s.segment} ${i===selected?s.active:''}`;btn.textContent=seg.label;btn.onclick=()=>{c.querySelectorAll('.'+s.segment).forEach((b,j)=>b.classList.toggle(s.active,j===i));indicator.style.left=`${(i/segments.length)*100}%`;onChange(seg.value,i);};c.appendChild(btn);});return c;}