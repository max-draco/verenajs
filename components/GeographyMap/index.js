import s from './index.module.css';
export function createGeographyMap({data=[],height=300}){const c=document.createElement('div');c.className=s.map;c.style.height=height+'px';const total=data.reduce((a,d)=>a+d.value,0);c.innerHTML=`<div class="${s.placeholder}">🗺️ Map Visualization</div><div class="${s.legend}">${data.slice(0,5).map(d=>`<div class="${s.item}"><span class="${s.country}">${d.country}</span><div class="${s.bar}"><div class="${s.fill}" style="width:${(d.value/total)*100}%"></div></div><span class="${s.pct}">${((d.value/total)*100).toFixed(1)}%</span></div>`).join('')}</div>`;return c;}