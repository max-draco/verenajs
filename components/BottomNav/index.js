import s from './index.module.css';
export function createBottomNav({items=[],activeId='',onChange=()=>{}}){const c=document.createElement('nav');c.className=s.bottomnav;items.forEach(item=>{const btn=document.createElement('button');btn.className=`${s.item} ${item.id===activeId?s.active:''}`;btn.innerHTML=`<span class="${s.icon}">${item.icon}</span><span class="${s.label}">${item.label}</span>${item.badge?`<span class="${s.badge}">${item.badge}</span>`:''}`;btn.onclick=()=>onChange(item.id);c.appendChild(btn);});return{el:c,setActive:(id)=>{c.querySelectorAll('.'+s.item).forEach((t,i)=>t.classList.toggle(s.active,items[i].id===id));}};}