import s from './index.module.css';
export function createShareCard({shareCount=0,avgPrice=0,currentPrice=0,side='yes'}){const value=(shareCount*currentPrice/100).toFixed(2);const cost=(shareCount*avgPrice/100).toFixed(2);const pnl=(value-cost).toFixed(2);const pnlPct=cost>0?((pnl/cost)*100).toFixed(1):0;const c=document.createElement('div');c.className=s.card;c.innerHTML=`<div class="${s.side} ${s[side]}">${side.toUpperCase()}</div><div class="${s.shares}">${shareCount.toFixed(2)} shares</div><div class="${s.row}"><span>Avg Price</span><span>${avgPrice.toFixed(0)}¢</span></div><div class="${s.row}"><span>Current</span><span>${currentPrice.toFixed(0)}¢</span></div><div class="${s.row}"><span>Value</span><span>$${value}</span></div><div class="${s.pnl} ${pnl>=0?s.profit:s.loss}">${pnl>=0?'+':''}$${pnl} (${pnlPct}%)</div>`;return c;}