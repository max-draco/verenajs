import s from './index.module.css';export function createPanel({title='',children=null,collapsible=false}){const c=document.createElement('div');c.className=s.panel;let collapsed=false;const render=()=>{c.innerHTML=`<div class="${s.header}"><span class="${s.title}">${title}</span>${collapsible?`<button class="${s.toggle}">${collapsed?'+':'−'}</button>`:''}</div><div class="${s.body}" style="${collapsed?'display:none':''}"></div>`;if(children)c.querySelector('.'+s.body).appendChild(children);if(collapsible)c.querySelector('.'+s.toggle).onclick=()=>{collapsed=!collapsed;render();};};render();return c;}