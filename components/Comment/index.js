import s from './index.module.css';
export function createComment({user={},content='',time='',likes=0,replies=[],onLike=()=>{},onReply=()=>{}}){const c=document.createElement('div');c.className=s.comment;c.innerHTML=`<img class="${s.avatar}" src="${user.avatar||''}" alt=""><div class="${s.body}"><div class="${s.header}"><span class="${s.name}">${user.name}</span><span class="${s.time}">${time}</span></div><div class="${s.content}">${content}</div><div class="${s.actions}"><button class="${s.action}" data-action="like">❤️ ${likes}</button><button class="${s.action}" data-action="reply">💬 Reply</button></div>${replies.length?`<div class="${s.replies}"></div>`:''}</div>`;c.querySelector('[data-action="like"]').onclick=onLike;c.querySelector('[data-action="reply"]').onclick=onReply;const repliesContainer=c.querySelector('.'+s.replies);if(repliesContainer){replies.forEach(r=>repliesContainer.appendChild(createComment(r)));}return c;}