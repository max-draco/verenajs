import s from './index.module.css';
export function createFilePreview({file=null,name='',size=0,type=''}){const c=document.createElement('div');c.className=s.preview;const icons={'image':'🖼️','video':'🎬','audio':'🎵','pdf':'📄','default':'📎'};const getIcon=t=>{if(t.startsWith('image'))return icons.image;if(t.startsWith('video'))return icons.video;if(t.startsWith('audio'))return icons.audio;if(t.includes('pdf'))return icons.pdf;return icons.default;};const formatSize=b=>{if(b<1024)return b+'B';if(b<1048576)return(b/1024).toFixed(1)+'KB';return(b/1048576).toFixed(1)+'MB';};c.innerHTML=`<div class="${s.icon}">${getIcon(type)}</div><div class="${s.info}"><div class="${s.name}">${name}</div><div class="${s.meta}">${formatSize(size)} • ${type.split('/')[1]||type}</div></div>`;return c;}