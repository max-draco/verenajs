import s from './index.module.css';
export function createStepper({steps=[],current=0,orientation='horizontal'}){const c=document.createElement('div');c.className=`${s.stepper} ${s[orientation]}`;steps.forEach((step,i)=>{const el=document.createElement('div');el.className=`${s.step} ${i<current?s.completed:''} ${i===current?s.active:''}`;el.innerHTML=`<div class="${s.number}">${i<current?'✓':i+1}</div><div class="${s.info}"><div class="${s.title}">${step.title}</div>${step.description?`<div class="${s.desc}">${step.description}</div>`:''}</div>`;c.appendChild(el);if(i<steps.length-1){const line=document.createElement('div');line.className=`${s.line} ${i<current?s.completed:''}`;c.appendChild(line);}});return c;}