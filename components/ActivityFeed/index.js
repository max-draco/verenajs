import s from './index.module.css';
export function createActivityFeed({activities=[],onItemClick=()=>{}}){const c=document.createElement('div');c.className=s.feed;activities.forEach(a=>{const el=document.createElement('div');el.className=s.item;el.innerHTML=`<img class="${s.avatar}" src="${a.user?.avatar||''}" alt=""><div class="${s.content}"><div class="${s.text}"><strong>${a.user?.name||'User'}</strong> ${a.action}</div><div class="${s.time}">${a.time}</div></div>`;el.onclick=()=>onItemClick(a);c.appendChild(el);});return{el:c,prepend:(a)=>{const el=document.createElement('div');el.className=`${s.item} ${s.new}`;el.innerHTML=`<img class="${s.avatar}" src="${a.user?.avatar||''}" alt=""><div class="${s.content}"><div class="${s.text}"><strong>${a.user?.name||'User'}</strong> ${a.action}</div><div class="${s.time}">${a.time}</div></div>`;c.insertBefore(el,c.firstChild);}};}